quarkus:
  datasource:
    devservices:
      container-env:
        "PAGER": "cat"
omgcluster:
  init:
    version: 2
  instance:
    address:
      internal: http://localhost:8080
      external: http://localhost:8080
