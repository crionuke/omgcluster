quarkus:
  datasource:
    devservices:
      container-env:
        "PAGER": "cat"
omgcluster:
  init:
    version: 2
  server:
    name: localhost
  tick:
    interval: 1000
  executor:
    zone:
      delay: 500
